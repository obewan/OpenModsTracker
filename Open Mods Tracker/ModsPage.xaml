<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenModsTracker.ModsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpenModsTracker"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Padding="28" Spacing="20">
            <Border Style="{StaticResource SectionCardStyle}">
                <Grid ColumnSpacing="18">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Spacing="6">
                        <TextBlock Text="Portfolio" FontSize="28" FontWeight="SemiBold" />
                        <TextBlock Text="Toutes les fiches suivies, triées par performance de downloads puis endorsements." Opacity="0.72" TextWrapping="WrapWholeWords" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center" Spacing="10">
                        <Button Content="Rafraichir" Click="RefreshButton_Click" IsEnabled="{Binding CanRefresh}" />
                        <TextBlock Text="{Binding CollectionSummary}" Opacity="0.68" HorizontalAlignment="Right" />
                    </StackPanel>
                </Grid>
            </Border>

            <InfoBar Title="Etat du portfolio" Message="{Binding StatusMessage}" IsOpen="{Binding HasStatusMessage}" Severity="Informational" IsClosable="False" />

            <ListView ItemsSource="{Binding Mods}" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:PortfolioMod">
                        <Border Style="{StaticResource SectionCardStyle}" Margin="0,0,0,14">
                            <Grid ColumnSpacing="18">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Spacing="10">
                                    <Grid ColumnSpacing="12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding DisplayName}" FontSize="22" FontWeight="SemiBold" />
                                            <TextBlock Text="{Binding Reference.GameDomain}" Opacity="0.6" />
                                        </StackPanel>
                                        <Border Grid.Column="1" Background="#1F3B2F" CornerRadius="999" Padding="10,4">
                                            <TextBlock Text="{Binding DisplayStatus}" />
                                        </Border>
                                    </Grid>

                                    <TextBlock Text="{Binding DisplaySummary}" TextWrapping="WrapWholeWords" MaxLines="3" Opacity="0.8" />

                                    <Grid ColumnSpacing="16" RowSpacing="10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <StackPanel>
                                            <TextBlock Text="Downloads" Opacity="0.65" />
                                            <TextBlock Text="{Binding DisplayDownloads}" FontSize="20" FontWeight="SemiBold" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="Unique DLs" Opacity="0.65" />
                                            <TextBlock Text="{Binding DisplayUniqueDownloads}" FontSize="20" FontWeight="SemiBold" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="Endorsements" Opacity="0.65" />
                                            <TextBlock Text="{Binding DisplayEndorsements}" FontSize="20" FontWeight="SemiBold" />
                                        </StackPanel>
                                        <StackPanel Grid.Row="1">
                                            <TextBlock Text="Views" Opacity="0.65" />
                                            <TextBlock Text="{Binding DisplayViews}" FontSize="18" FontWeight="SemiBold" />
                                        </StackPanel>
                                        <StackPanel Grid.Row="1" Grid.Column="1">
                                            <TextBlock Text="Commentaires" Opacity="0.65" />
                                            <TextBlock Text="{Binding DisplayComments}" FontSize="18" FontWeight="SemiBold" />
                                        </StackPanel>
                                        <StackPanel Grid.Row="1" Grid.Column="2">
                                            <TextBlock Text="Bugs" Opacity="0.65" />
                                            <TextBlock Text="{Binding DisplayBugs}" FontSize="18" FontWeight="SemiBold" />
                                        </StackPanel>
                                    </Grid>

                                    <StackPanel Orientation="Horizontal" Spacing="18">
                                        <TextBlock Text="{Binding AuthorLabel}" />
                                        <TextBlock Text="{Binding DisplayVersion}" />
                                        <TextBlock Text="{Binding UpdatedAtLabel}" />
                                        <TextBlock Text="{Binding UploadedAtLabel}" />
                                    </StackPanel>
                                </StackPanel>

                                <Button Grid.Column="1" Content="Ouvrir sur Nexus" Tag="{Binding PageUrl}" Click="OpenModButton_Click" VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </ScrollViewer>
</Page>
